<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.growth.streetwarrior" >

    <application
        android:name=".StreetWarriorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreetWarrior" >
        <activity
            android:name="com.growth.streetwarrior.presentation.ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.StreetWarrior.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.growth.streetwarrior.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.StreetWarrior.NoActionBar" >
        </activity>
    </application>

</manifest>